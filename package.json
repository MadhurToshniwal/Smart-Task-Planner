{
  "name": "smart-task-planner",
  "version": "1.0.0",
  "description": "AI-driven task planning application",
  "main": "index.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "build": "npm run install-all && npm run build-frontend && node copy-frontend.js",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "task",
    "planner",
    "ai"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  }
}