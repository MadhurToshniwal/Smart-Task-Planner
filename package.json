{
  "name": "smart-task-planner",
  "version": "1.0.0",
  "description": "AI-driven task planning application",
  "main": "index.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "build": "npm run install-deps && cd frontend && npm run build && mkdir -p ../backend/public && cp -r build/* ../backend/public/",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install",
    "railway-deploy": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "task",
    "planner",
    "ai"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}